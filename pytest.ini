[pytest]
# The test suite lives in tests/ and protocol/. scripts/ holds operational
# tooling, not tests: scripts/smoke_test.py is a live-API credential check
# whose name pytest would otherwise collect, and which cannot run in a
# reviewer environment. Setting testpaths makes a bare `pytest` equivalent
# to the documented `pytest tests/ protocol/`.
testpaths = tests protocol
